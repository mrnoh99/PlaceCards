# PinSpots 인수인계 — 현재 상태

> 새 Claude 세션이 **가장 먼저 읽을 문서**입니다.
> `00_프로젝트종합가이드_새세션용.md`는 2026년 **기획 단계** 기록이라 지금과
> 어긋나는 결정이 많습니다(Vision OCR 제거, 백엔드 프로토타입 등). 배경이
> 궁금할 때만 참고하세요.

| | |
|---|---|
| 기준 커밋 | `dbf39e8` (main), CHANGELOG **188차** |
| 버전 / 빌드 | 1.0 / 4 |
| 배포 타깃 | iOS 17.0 |
| 열린 PR | 없음 (#1–#43 전부 병합됨) |
| Swift 파일 | 70개 |

> **지금 살아 있는 문제 하나**: 지도 탭의 **Naver 지도가 깨져 있습니다.**
> 원인 분석이 §7에 있습니다. 이어서 작업한다면 거기서 시작하세요.

---

## 1. 이 앱이 무엇인가

**PinSpots** — 인스타그램·지도앱에서 본 장소를 카드로 모으는 iOS 앱.
SwiftUI + MVVM, **백엔드 서버 없음**, 로컬 JSON 저장.

외부 서비스는 전부 **BYOK**(사용자가 자기 API 키를 설정에 입력)입니다.
Google Places, Naver, AI 제공자(Claude/OpenAI/Gemini/Gateway) 모두 기기에서
직접 호출합니다. 앱에 내장된 키는 하나도 없습니다.

카드를 만드는 경로는 셋입니다: **공유 시트**(지도앱 링크/스크린샷),
**사진 스캔**(AI 또는 기기 내 OCR), **Google Takeout 가져오기**.

---

## 2. 절대 어기면 안 되는 것 5가지

이건 취향이 아니라, 어기면 **사용자 데이터가 날아가거나 돈이 새는** 항목입니다.

### ① `SourceType`의 case를 절대 지우지 마라
`Models/MediaModels.swift`. `MediaItem.source`가 non-optional이라 **디코딩이
한 번 실패하면 `loadPlaceCards()`가 배열 전체를 버립니다.** 저장된 카드가
쓰는 case를 지우면 라이브러리가 통째로 사라집니다. 안 쓰는 case는 지우지
말고 주석으로 표시하세요. (파일에 이 규칙이 명시돼 있습니다.)

### ② Google 키에 "iOS 앱" 애플리케이션 제한을 걸지 마라
지도 탭의 Google 지도는 **Maps JavaScript API를 `WKWebView`에** 띄웁니다.
웹 페이지는 referer로 검사되고 REST 호출은 `X-Ios-Bundle-Identifier`로
검사되는데, **키 하나에는 제한을 한 종류만** 걸 수 있습니다. "iOS 앱"을
고르면 지도 탭이 백지가 됩니다. 현재 운영 방식은 **제한 없음 + API별 일일
할당량**입니다. 자세한 건 `README.md`의 "Google 키 하나가 두 가지 방식으로
호출됩니다" 절.

### ③ Geocoding API를 활성화하지 마라
쓰지 않습니다. 주소→좌표도 `places:searchText`가 처리합니다
(`PlaceSearchService.geocodeAddress`). 켜면 할당량 없는 표면만 하나 늘어납니다.

### ④ 비용은 **필드 마스크**가 결정한다
Places API (New)는 요청한 필드에 따라 SKU 티어가 갈립니다.
`places.id`=무료, `places.location`/`formattedAddress`=Essentials,
`displayName`=Pro, `rating`/`regularOpeningHours`=**Enterprise**.
필드를 무심코 추가하면 티어가 올라갑니다.

**자동 검증 1행 = Text Search 2회**입니다(장소 검색 + 주소 지오코딩).
`maxAutoVerifiedRows = 10`이 그래서 있습니다.

### ⑤ 새 UI 문자열은 `Localization.swift`에 반드시 넣어라
`"한국어".localized` 패턴이고, 번역 사전은 플랫 Swift 딕셔너리입니다.
누락되면 영어 기기에서 한국어가 그대로 노출됩니다.
`python3 Tools/checks/check.py`가 MISSING/UNUSED를 잡아 줍니다.

**앱 화면 언어는 이제 iOS 설정을 따릅니다**(188차). 설정에 "앱 언어"
선택이 없고, `AppLanguage.current()`가 `Locale.preferredLanguages`를
읽습니다. 저장된 값이 없으므로 **런타임에 언어가 바뀌지 않습니다** — iOS가
언어 변경 시 앱을 재실행하기 때문입니다. 그래서 `LocalizationObserver`와
앱 루트의 `.id()` 리마운트가 제거됐습니다. 되살리지 마세요.

예외: `Services/LegalDocuments.swift`(방침·약관 전문)는 **일부러**
`.localized`를 거치지 않고 언어별 병렬 문서로 씁니다. 문단을 사전 키로
쓰면 사전이 읽을 수 없게 되고 두 언어를 대조할 수도 없습니다.

---

## 3. 작업 방식

### 브랜치와 PR
```
git fetch origin "refs/heads/main:refs/remotes/origin/main" --force
git checkout -B <본인 세션 브랜치> origin/main
# 작업 → 검증 → CHANGELOG → 커밋 → 푸시 → PR
```

**`remote.origin.fetch`가 비어 있습니다.** 그래서 일반 `git fetch origin`이
추적 참조를 갱신하지 않습니다. 위처럼 refspec을 명시하세요.

**병합은 사용자가 명시적으로 지시할 때만** 합니다. squash 병합.

**PR을 열지 않으면 빌드 검증이 없습니다.** CI는 `main` 푸시와 pull
request에서만 돕니다(`.github/workflows/ci.yml`). 브랜치에 푸시만 해서는
아무것도 컴파일되지 않습니다.

**squash 병합 때문에 다음 세션의 브랜치가 충돌합니다.** 직전 작업이 squash로
main에 들어가면 같은 내용이 서로 다른 히스토리로 존재하게 됩니다. main에서
새로 분기하지 않고 이어서 작업하면 PR이 `dirty` 상태가 됩니다 — 그때는
`git merge origin/main`으로 해결하세요(브랜치 히스토리를 다시 쓰지 말 것).

**브랜치 삭제는 이 환경에서 안 됩니다.** `git push origin --delete`가 403으로
거부되고 GitHub MCP에도 삭제 도구가 없습니다. 커밋 푸시는 정상입니다.
정리가 필요하면 사용자에게 명령을 넘기세요.

커밋 메시지 끝에 붙일 서명은 세션마다 시스템이 알려주는 값을 쓰세요
(모델 이름이 세션마다 다릅니다).

커밋 메시지 끝:
```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: <세션 URL>
```

### CHANGELOG
`## [Unreleased]` 바로 아래에 **차수 내림차순**으로 추가합니다
(최신이 위). 형식은 기존 항목을 보고 맞추세요 — 무엇을 했는지보다
**왜 그렇게 했는지**를 적는 스타일입니다.

### 검증 (커밋 전 필수)
로컬에 Xcode가 없습니다. 쓸 수 있는 검사는 둘뿐입니다:

```sh
python3 Tools/checks/check.py            # 괄호 균형 + 번역 MISSING/UNUSED
python3 Tools/ocr-regression/check.py    # OCR 회귀 50건
```

**기준선 (main `dbf39e8`)**: 괄호 오탐 2건(`LinkMetadataFetcher`,
`WebsiteBusinessInfoFetcher` — 정규식 리터럴의 `]`를 파서가 오인),
번역 MISSING 5 · UNUSED 6. **이보다 나빠지지 않으면 통과**입니다. 자세한 건
`Tools/checks/README.md`.

> **이 검사들은 `import` 누락을 못 잡습니다.** PR #36의 `CSVExport.swift`
> (`import SwiftUI` 누락)와 188차의 `SettingsDetailViews.swift`
> (`UniformTypeIdentifiers` 누락) 둘 다 여기서 안 잡혔습니다.
> **CI가 유일한 컴파일 방어선입니다.**

**새 Swift 파일을 만들면 `project.pbxproj`에 직접 등록해야 합니다.** 이
프로젝트는 파일시스템 동기화 그룹을 쓰지 않아서, 등록하지 않은 파일은 그냥
컴파일되지 않습니다. 네 군데 전부 넣어야 합니다 — `PBXBuildFile`,
`PBXFileReference`, 해당 그룹의 `children`, `Sources` 빌드 페이즈.
(188차에 파일 4개를 이렇게 추가했고 CI로 확인했습니다.)

### CI
`.github/workflows/ci.yml` — macOS 러너에서 `xcodebuild build`만 돌립니다.
**테스트 타깃이 없습니다.** OCR 회귀 검사도 CI가 돌리지 않으니 직접 실행하세요.

---

## 4. 최근 작업 (PR #28–#43, 전부 병합됨)

### PR #42–#43 (가장 최근 세션)

| PR | 내용 |
|---|---|
| #42 | 인수인계 문서(이 문서) 최초 작성 |
| #43 | **지출 카운터 · iCloud 동의 · 방침/약관 · 설정 재구성** (187·188차) |

#43이 무엇을 왜 했는지는 §4.1에 따로 적었습니다. 이어서 작업한다면
그 결정들을 뒤집기 전에 읽어보세요.

### PR #28–#41

| PR | 내용 |
|---|---|
| #28 | Places 비용 절감 — 카드당 Place Details 호출 제거 |
| #29 | 기기 내 OCR 도입 (AI 키 없는 사용자용) |
| #30 | 인스타 번호 목록 인식 + 스캔 비용 상한 |
| #31 | 좌표만 읽는데 Enterprise 요금 내던 것 수정 (필드 마스크 분리) |
| #32 | 빌드 번호 4 |
| #33 | 라이브러리 저장을 메인 스레드 밖으로 |
| #34 | 키보드 갇힘, 조용한 키 누락, 오해 소지 있는 출처 표기 수정 |
| #35 | 아이콘 전용 컨트롤 VoiceOver 라벨 |
| #36 | CSV 내보내기 |
| #37 | 설정 하단 크레딧 |
| #38 | API 키와 함께 번들 ID 전송 (`X-Ios-Bundle-Identifier`) |
| #39 | Google Takeout 가져오기 (**API 호출 0회**) |
| #40 | Google 키 설정 문서화 — 제한이 아니라 할당량 |
| #41 | **기기 내 스캔을 주소 기준으로 재작성** |

---

## 4.1 PR #43이 한 것 — 그리고 왜 그렇게 했는가

출발점은 "전반적으로 평가하고 사용자 입장에서 필요한 부분을 검토해라"였고,
결론은 **이 앱이 사용자에게 숨기고 있던 것이 셋**이라는 것이었습니다. 돈을
쓰고 있다는 사실, iCloud에 데이터를 올리고 있다는 사실, 사진이 제3자에게
전송된다는 사실. 셋 다 앱은 알고 있었고 사용자만 몰랐습니다.

### 새로 생긴 것

| 파일 | 무엇 |
|---|---|
| `Services/APIUsageCounter.swift` | Google 검색/상세/사진 + AI 요청을 오늘·이번 달로 셈 |
| `Services/LegalDocuments.swift` | 방침·약관 전문(한/영 병렬) |
| `Views/LegalDocumentView.swift` | 위 문서를 읽는 화면 |
| `Views/SettingsDetailViews.swift` | 설정 하위 화면 4개 |
| `PRIVACY_POLICY.md` `TERMS_OF_SERVICE.md` | 위 전문과 **같은 내용**(호스팅용) |

### 뒤집기 전에 알아야 할 결정들

- **사용량 카운터는 성공한 요청만 셉니다.** 청구서가 아니라 근사치이고,
  실패한 제공자까지 세면 모든 숫자가 의심스러워집니다. 지오코딩은 검색에
  합산합니다 — Google이 같은 `SearchTextRequest`로 과금하므로, 나누면
  사용자가 콘솔에 건 할당량과 숫자가 대응하지 않게 됩니다.
- **iCloud 자동 보관 기본값은 켜짐입니다.** 데이터가 개발자 서버가 아니라
  사용자 본인 iCloud로 가고, 일괄로 끄면 재설치·기기 교체 시 이걸 이미
  의지하던 사람의 안전망을 역시 묻지 않고 치우는 게 됩니다. 빠졌던 건
  기본값이 아니라 **거절할 수단**이었습니다. 끄면 저장된 사본도 삭제합니다.
  미설정 키를 `bool(forKey:)`가 아니라 `object(forKey:)`로 읽는 이유 —
  전자는 스위치 생기기 전 설치본을 전부 "껐음"으로 읽습니다.
- **방침·약관은 링크가 아니라 앱 안에 전문을 실었습니다.** 링크는 사람이
  찾을 때 실패하는 유일한 형태이고(비행기, 캡티브 포털, 페이지 이동),
  가장 고지할 가치가 있는 사실(AI 스캔이 이미지를 제3자에게 업로드한다)은
  그 스캔을 제안하는 화면에서 확인할 수 있어야 합니다.
  **`.md` 두 개와 Swift 원본은 같은 내용이어야 합니다 — 한쪽만 고치지 마세요.**
- **설정 순서는 언어 → AI → Google → Naver입니다.** 구현 순서가 아니라
  중요도 순입니다. AI 키가 첫 입력란인 이유는 이 앱의 핵심이 "사진에서
  장소를 찾는 일"이고 그걸 하는 게 그 키이기 때문입니다. Naver는 지역이
  KR이거나 선호 언어에 한국어가 있을 때만 보입니다.
- **`ScanResultLanguage` 기본값도 기기를 따릅니다.** 안 그러면 영어 기기
  사용자가 영어 앱에서 한국어 카테고리·메모를 받습니다.

### 남긴 흔적

설정에서 **개발자용 문구를 들어냈습니다** — 사용자에게 Xcode에서 Signing &
Capabilities를 확인하라고 안내하고 있었습니다. `SharedImportStore`의
`recordDebugStatus`는 남아 있지만 이제 앱이 읽지 않습니다(App Group
컨테이너에서 직접 꺼내 보세요).

---

## 5. OCR 재작성 (#41) — 이어서 할 가능성이 가장 높은 부분

### 무엇이 바뀌었나
**이름을 크기로 먼저 고르던 방식 → 주소를 먼저 찾고 이름을 역추적.**

```
주소 위치를 찾는다 (한국 주소 정규식은 결정적, 이름은 패턴이 없음)
  ① 같은 행의 왼쪽 열        (표, 배지가 오른쪽에 붙은 목록)
  ② 주소가 있는 줄의 앞부분   (함흥곰보냉면 | 안양시 동안구 귀인로190번길 23)
  ③ 위쪽 행 역추적           (마부시 / 안양시 동안구 시민대로 280)
주소가 없으면 → 아무것도 내놓지 않고 AI를 안내한다
```

Vision의 `boundingBox`로 행·열을 판단합니다. 크기 휴리스틱
(`listItems`, `singlePlace`)은 **제거**했습니다.

### 왜 이렇게 했나 — 실제 캡처 11장 측정

| 세트 | 기존 | 변경 후 |
|---|---|---|
| 인스타 게시물 15곳 | 6곳 이름 소실 | 15/15 |
| 홀드아웃 9건 | — | 9/9 |
| 인천 번호 목록 8곳 | 배지를 집음 | 8/8 |
| 블루리본 3열 표 18곳 | **0/18** | 18/18 |

**좌표가 결정적입니다.** 같은 입력에 텍스트 규칙만 쓰면 표 3/11, 인천 0/8.

### 회귀 검사
`Tools/ocr-regression/` — 픽스처 4세트 + 체커 50건. README에 각 세트가
무엇을 보는지 적혀 있습니다.

> **`_scanner_port.py`는 Swift의 거울입니다.** `ScreenshotPlaceScanner.swift`의
> 규칙이나 상수를 고치면 **반드시 같이 고치세요.** 안 그러면 존재하지 않는
> 코드를 검사하게 됩니다.

만드는 동안 이 세트가 두 번 실수를 잡았습니다 — 주소 패턴에 `읍/면`을
허용했더니 **`평양냉면`의 "면"**이 행정구역으로 잡혔고, 행정구역 조각 제거
규칙을 넣었더니 **`함흥곰보냉면`·`마부시`·`양수면옥`**이 같이 죽었습니다.
정규식을 건드리면 반드시 전체 세트를 돌리세요.

### 측정하지 못한 것
**이 검사는 OCR 인식 정확도를 재지 않습니다.** Vision을 이 환경에서 실행할
수 없어, 픽스처는 **인식이 끝난 뒤의 텍스트·좌표**에서 시작합니다. 좌표가
실측인 두 세트도 좌표만 실측이고 텍스트는 보정입니다(tesseract로 좌표를
얻었으나 한국어 인식 품질이 Vision과 다릅니다).

### 알려진 한계 (고치지 못함)
지도 앱 장소 화면처럼 제목과 주소 사이에 카테고리 줄이 있으면 그 줄을
집습니다 — `스타벅스 강남대로점` 대신 `카페`. "가장 큰 후보"로 바꾸면 이
건은 풀리지만 **섹션 제목이 항목보다 큰 번호 목록이 깨집니다**(`인천`).
양쪽을 재 보고 가까운 쪽을 택했습니다. 그 행도 주소는 올바로 들고 가므로
Google 검증이 확정을 거부하고 사용자에게 남깁니다.

---

## 6. 다음에 할 일

> **순서 제안**: §7의 Naver 지도 버그와 아래 "방침 게시"가 **같은 작업
> (`docs/` + GitHub Pages)으로 함께 해결됩니다.** 거기서 시작하는 게
> 가장 효율적입니다.

### 🔴 출시 차단
- **Naver 지도가 깨져 있음** — §7 참고. 기능 버그이자, 고치는 방법이
  아래 방침 게시와 겹칩니다.
- ~~**개인정보처리방침 · 이용약관**~~ — 187차에서 작성하고 연락처
  (`jsnoh2010@gmail.com`)까지 채움(`PRIVACY_POLICY.md`,
  `TERMS_OF_SERVICE.md`, 앱 안에서도 전문 열람 가능). **남은 것 하나**:
  `.md`를 GitHub Pages 등에 **게시하고 그 URL을 App Store Connect에
  등록**할 것. 문서에 적힌 주소는 실제로 확인하는 곳이어야 합니다.
- **심사자용 데모 키** — BYOK라 **심사자는 키가 없습니다.** 지금 상태로
  제출하면 심사자 화면에서 주요 기능이 전부 빈칸으로 보입니다. App Review
  Notes에 테스트용 키(또는 그 기능이 키 없이 동작하지 않는다는 설명과
  대체 확인 방법)를 반드시 넣으세요. 넣지 않으면 지침 2.1로 반려될
  가능성이 높습니다.
- **개인정보 영양성분표(App Store Connect)** — 사용자 **사진이 AI 제공자로
  전송**됩니다. 이 질문지를 사실대로 채우지 않으면 허위 신고입니다.
  무엇이 어디로 가는지는 `PRIVACY_POLICY.md`의 "외부로 전송되는 정보"에
  전부 정리돼 있으니 그대로 옮기면 됩니다.
- **상표 검색** — "PinSpots" (기획 문서 1.1 항목). 일반 웹 검색으로는
  소프트웨어 분류에 동일 상표가 보이지 않았고 "pinspot"은 조명 기구의
  일반명사로 쓰입니다. 다만 **공식 DB(키프리스·USPTO)를 확인하지
  못했으므로 미수행으로 간주하세요.** 한국 앱이니 키프리스에서 9류·42류를
  먼저 보는 것이 순서입니다.
- **`ITSAppUsesNonExemptEncryption` 미설정** — 사소하지만 업로드할 때마다
  암호화 질문을 받습니다.

### 🟡 기기 검증 (사용자만 가능)
개발 환경에 Xcode가 없어 제가 못 하는 것들입니다.

1. **#41 OCR 실동작** — 3열 표에서 Vision이 셀을 열별로 나눠 주는가(제 검증의
   대전제), 배지가 이름과 같은 행으로 묶이는가, 주소 없는 목록에서 빈손 +
   AI 안내가 제대로 뜨는가
2. **OCR 인식 정확도** — 기획 목표 스크린샷 50~100장, 80%
3. **저장 성능** — 카드 500 / 1000 / 5000개 (기획 3.2)
4. **Takeout** — 실제 내보내기 파일 (GeoJSON, 목록별 CSV)
5. **#27** — 요일 순서, 영업중 배지
6. **#43 UI 전반** — 컴파일만 확인됐고 **화면 동작은 아무도 안 봤습니다.**
   설정 새 순서와 하위 화면 이동, 영어 기기에서 앱이 영어로 뜨는지,
   한국어 기기에서 Naver 섹션이 보이는지, AI 키 미등록 경고,
   iCloud 토글을 껐을 때 사본이 실제로 삭제되는지, 사용량 카운터가
   실제로 증가하는지.

✅ 검증 완료: 빈 공유 시트(#23), Google 지도 탭 표시,
Naver 지도 **타일 렌더링·인증**(§7 — 핀과 중심은 여전히 깨져 있음)

### 🟢 Google Cloud Console
`pinspots-prod` 기준. 앞의 4개는 완료, 뒤의 2개가 남았습니다.

| 항목 | 상태 |
|---|---|
| Places API (New) 활성화 | ✅ |
| Maps JavaScript API 활성화 | ✅ (지도 탭 표시 확인됨) |
| 메서드별 일일 할당량 7개 | ✅ `SearchTextRequest` 200 / `GetPhotoMediaRequest` 200 / `GetPlaceRequest` 50 / 나머지 4개 0 |
| 애플리케이션 제한 = 없음 | ✅ (②번 항목 참고) |
| API 제한 = Places (New) + Maps JS | ⬜ |
| 예산 $5 + 50/90/100% 알림 | ⬜ **제한이 아니라 할당량으로 막는 구성이라 사실상 유일한 안전망** |

최대 일일 노출액 약 **$9.40**.

### ⚪ 보류 (이유 있음)
- **CLGeocoder 도입** — #31로 지오코딩 비용이 내려가 우선순위 하락. 사용자가
  올린 가이드 문서에 사실 오류가 있었음("로컬 처리", "초당 100회"는 부정확).
- **`nameByLanguage` / `addressByLanguage`** — 다국어 필드 분리
- **Dynamic Type · 고대비 대응**
- **Google 키 공급 방식** — 지금은 BYOK. 키 내장+할당량 vs 프록시 서버는 미결정.
- **지도 탭 검색 시 웹뷰 리로드** — Google 지도에서 한 글자 입력마다 지도가
  다시 로드됨(무료 10,000회/월 안에선 무시 가능). JS로 마커만 갱신하면 해소.
- **OCR 키 분리(선택지 C)** — 지도용 키는 리퍼러 제한, Places용은 iOS 앱 제한.
  설정이 키 입력을 하나만 받으므로 코드 변경 필요.

---

## 7. 🔴 지금 깨져 있는 것 — Naver 지도

**증상**(사용자 스크린샷으로 확인): 지도 탭에서 Naver를 고르면 지도는
정상적으로 그려지는데 **발칸반도가 중심이고 핀이 하나도 없습니다.**

### 배제된 원인

타일이 그려지고 `© NAVER Corp.` 저작권 표시와 NAVER 로고까지 보입니다.
따라서 **인증·네트워크는 정상**입니다 — NCP Web Service URL 문제도,
`ncpClientId`→`ncpKeyId` 파라미터 문제도, 페이지 404도 아닙니다.
설정의 Client ID도 등록돼 있습니다(없으면 "Naver Client ID가 필요합니다"가
뜹니다).

### 남은 원인: 데이터 주입 실패

`NaverMapWebView.swift`는 페이지를 로드한 뒤
`window.renderNaverMap(payload)`를 주입합니다. 지도가 자기 기본 화면을
보여준다는 건 **이 주입이 안 먹혔다**는 뜻입니다.

```swift
// NaverMapWebView.swift:129 — 완료 핸들러가 없습니다
webView.evaluateJavaScript("window.renderNaverMap(\(json));")
```

**JS 오류가 통째로 삼켜집니다.** 함수가 없든 페이로드를 거부하든 앱은
알 수 없고 사용자에게도 말하지 못합니다. 이게 이 버그가 조용했던 이유이고,
**무엇을 고치든 여기에 완료 핸들러부터 다세요.**

유력한 가설 두 가지(확인 못 함):
1. **Peragra 페이지가 바뀌었다.** 함수 이름이나 페이로드 형태가 달라지면
   이쪽에서 할 수 있는 게 없습니다.
2. **`tripDestination`이 항상 빈 문자열이다.** Peragra에서는 항상 채워지는
   값입니다. 그 페이지가 장소가 없을 때 이 값으로 중심을 잡는다면 빈
   문자열이 엉뚱한 좌표를 만들 수 있습니다.

### 근본 문제

```swift
// NaverMapWebView.swift:54
https://mrnoh99.github.io/Peragra/naver-map-embed.html
```

**PinSpots의 지도가 다른 프로젝트(Peragra)의 웹페이지에 의존합니다.** 이
저장소에 그 페이지가 없고, 앱에서 고칠 수도 없습니다. 파일 주석에도
"not editable from here"라고 적혀 있습니다. 그 페이지가 바뀌면 예고 없이
깨집니다 — 지금이 그 상황일 가능성이 높습니다.

### 권장 해결

**임베드 페이지를 이 저장소로 가져오세요.** `docs/naver-map-embed.html`에
자체 사본을 두고 `embedURL`을 `mrnoh99.github.io/PlaceCards/...`로 바꾸면
스크립트 URL·파라미터·중심 계산·마커를 전부 앱이 통제합니다.

마침 **방침 URL 때문에 어차피 이 저장소에 GitHub Pages를 켜야 합니다**(§6
🔴 참고). `docs/` 하나에 방침 페이지와 지도 페이지를 같이 올리면 출시 차단
항목과 이 버그가 한 번에 정리됩니다.

### 먼저 할 30초 확인

Apple/Google 지도로 전환해 핀이 보이는지 보세요. 거기서도 핀이 없으면
카드에 좌표가 없는 것입니다(`MapViewModel.annotatedPlaceCards`가 좌표 있는
카드만 거릅니다) — 그건 별개 문제입니다.

### 이 환경의 제약

`github.io` 접근이 프록시에 막혀 있어 **임베드 페이지를 읽을 수 없습니다.**
Xcode도 없어 앱 실행도 못 합니다. 페이지를 새로 쓴다면 기기 확인은
사용자에게 부탁해야 합니다.

---

## 8. 이 프로젝트에서 자주 밟는 함정

- **AI 응답을 그대로 믿지 말 것** — 이 세션에서 제가 네 번 틀렸습니다.
  OCR 설계 전제(지도앱 화면인 줄 알았으나 인스타 목록), 비용 추정(절반으로
  계산), 죽은 enum 제거 제안(①번 규칙 위반), 지연 사진 로딩 제안(목록 화면이
  어차피 썸네일을 그림). **코드를 먼저 읽고 판단하세요.**
- **`grep -c`는 0건일 때 exit 1** — `&&` 체인이 끊깁니다.
- **GeoJSON은 `[경도, 위도]`** — 이 앱의 다른 모든 API와 반대.
- **Takeout의 `[0,0]`** — 해결 못 한 장소에 0,0을 기록. 반드시 걸러야 함.
- **`Task {}`는 순서를 보장하지 않음** — `StorageService`가 세대 카운터를
  쓰는 이유.
- **Vision `boundingBox`는 원점이 좌하단** — `maxY`가 1에 가까우면 화면 위쪽.
- **새 Swift 파일은 `project.pbxproj` 네 곳에 직접 등록** — 안 하면 조용히
  컴파일에서 빠집니다(§3 참고).
- **`evaluateJavaScript`에 완료 핸들러를 빼먹지 말 것** — 웹뷰 쪽 오류가
  통째로 사라집니다. Naver 지도 버그(§7)가 조용했던 이유입니다.
- **GitHub API의 PR `merged` 필드를 믿지 말 것** — 필드를 일부만 요청하면
  병합된 PR도 `merged: false`로 옵니다. `git log`로 squash 커밋을 확인하세요.
- **이 환경에서 막히는 것들** — 브랜치 삭제(403), `github.io` 접근,
  공식 상표 DB(키프리스·USPTO), Xcode. 못 하는 걸 우회하려 들지 말고
  사용자에게 넘기세요.

---

## 9. 파일 지도

```
PlaceCards/PlaceCards/
├── Models/        PlaceCard, Board, MediaModels(①번 규칙), SourceModels
├── Services/      PlaceSearchService(Google), NaverPlaceSearchService,
│                  AIProvider(+Chain), ScreenshotPlaceScanner(#41),
│                  StorageService(로컬 JSON), BackupService, TakeoutImport,
│                  CSVExport, MapOpeners, Localization, KeychainService,
│                  APIUsageCounter(#43), LegalDocuments(#43),
│                  CloudBackupService(동의 스위치 있음)
├── ViewModels/    PlaceCardViewModel(가장 큼), Settings/Gallery/MapViewModel
└── Views/         25개. PlacesMapView(지도 3종), SettingsView(첫 화면만),
                   SettingsDetailViews(하위 4화면), LegalDocumentView,
                   EditPlaceCardSheet, ImportBoardSheet(Takeout)
PlaceCardsShare/   공유 시트 확장

Tools/checks/           괄호·번역 검사 (기준선은 그 README에)
Tools/ocr-regression/   #41 회귀 검사 50건
CHANGELOG.md            188차. 왜 그렇게 했는지가 적혀 있음 — 가장 가치 있는 문서
README.md               빌드 방법, BYOK 키 발급, Google 키 운영 방식
PRIVACY_POLICY.md       방침 — LegalDocuments.swift와 같은 내용이어야 함
TERMS_OF_SERVICE.md     약관 — 위와 같음
PLANNING/ RESEARCH/ IMPLEMENTATION/   기획 단계 원본
```

**막히면 CHANGELOG를 읽으세요.** 대부분의 "왜 이렇게 돼 있지?"에 대한 답이
거기 있습니다.
