# PinSpots 화면 — 지금의 모양과 밟으면 안 되는 것

**갱신 2026-09-23, 빌드 49 (백업 메모리 크래시 수정 2단계) 기준.**

이 문서는 원래 "UI 개편 시작 문서"였다. 개편은 PR #77·#78·#79로 끝났고,
그 뒤로도 이 문서가 **지금 화면과 지켜야 할 규칙**을 적는 자리로 남았다.
이름만 "개편"이다 — `CLAUDE.md`와 다른 문서들이 이 이름으로 가리키므로
파일 이름은 그대로 둔다.

§1은 지금 모양이고, §2의 목록은 개편을 지나면서 실제로 쓸모가 있었다.

- **§1** 지금 UI가 어떻게 생겼나
- **§2** 갈아엎어도 **반드시 살아남아야 하는 동작** ← 이 문서의 핵심
- **§3** 어디까지가 UI이고 어디부터가 아닌가
- **§4** UI로는 못 바꾸는 제약
- **§5** 이 코드베이스에서 실제로 밟은 SwiftUI 함정
- **§6** 문서 상태

같이 읽을 것: `CLAUDE.md`(작업 규칙, 반드시), `CHANGELOG.md`(왜 그렇게 됐는지).

---

## 1. 지금의 UI

### 1.1 탭 셋, 그리고 첫 탭은 두 칸

`MainTabView`의 `TabView` 하나. 탭 정의는 `Services/AppNavigation.swift`의 `AppTab`.

| 탭 | 화면 | 하는 일 |
|---|---|---|
| 갤러리 | `HomeView` (615줄) | 두 칸. 아래 설명 |
| 지도 | `PlacesMapView` (427줄) | Apple / Google / Naver 세 가지 지도 |
| 설정 | `SettingsView` (462줄) + `SettingsDetailViews` (425줄) | 언어·AI 키·Google·Naver·데이터 |

탭 이름은 "갤러리"지만 타입은 `HomeView`이고 태그도 `.home`이다. 이 저장소에
이미 다른 `GalleryView`가 있어서 이름을 옮기면 부딪힌다.

예전에는 탭이 넷이었고 갤러리가 따로 있었다. 첫 탭이 두 칸으로 갈라지면서
오른쪽 칸이 곧 갤러리가 되어, 탭으로 또 두면 같은 것이 두 군데가 된다.

### 1.2 첫 탭의 두 칸

`HomeView`는 `NavigationSplitView`다. 왼쪽은 고르는 곳, 오른쪽은 고른 것이
펼쳐지는 곳이다. 아이폰처럼 좁은 화면에서는 저절로 한 단으로 접혀, 줄을 누르면
오른쪽 칸이 밀려 들어온다.

```
PinSpots            ┐
  모든 카드          │
  가져오기           │ 왼쪽: List(selection:)
  삭제됨             │
카테고리별 보기       │
  [카페] [음식점]     │
사용자 보드          │
  제주도 맛집        ┘
```

고를 수 있는 것은 `HomeSelection`이다 — 갤러리로 펼쳐지는 범위 셋과, 갤러리가
아닌 삭제됨이 섞이기 때문이다.

오른쪽 칸은 `GalleryView`(1,038줄) 아니면 `TrashView`(161줄)다. **오른쪽 칸은
`NavigationStack`으로 감싸져 있다** — 그 안에서 카드 상세로 밀고 들어가기
때문이고, 빼면 §5의 `navigationDestination ... is misplaced` 항목이 그대로 재현된다. `GalleryView`는 그래서 제
스택을 세우지 않는다(`providesNavigationStack: false`).

### 1.3 무엇을 보여 줄지는 `AppNavigation`이 정한다

`AppNavigation`이 루트에서 한 번 주입되고 모든 화면이 `@EnvironmentObject`로
읽는다.

- `galleryScope` — `.all` / `.board(id)` / `.imported`. **바꾸는 곳은 왼쪽 목록의
  선택 하나뿐**이고 한 방향으로만 흐른다. 지도 탭도 이 값을 읽지만 보드만 알기
  때문에, `.imported`일 때는 `boardID`가 nil이라 좁히지 않는다.
- `liveSelection` — **지금 고르고 있는** 카드들. **지도를 좁히는 유일한
  길이다.** 지도 탭이 이 값을 읽어, 고른 채로 지도로 건너가면 고른 것만 보인다.
  거는 곳이 둘이고 둘 다 "지금 이것을 보고 있다"는 뜻이다:
  - 갤러리의 선택 모드(`GalleryView.syncLiveSelection`) — 선택을 놓으면 비워진다.
  - 카드 하나를 열어 둔 것(`GalleryView.selectedCard`) — 카드 하나도 하나를
    고른 것으로 친다.

  둘 다 `GalleryView.syncLiveSelection`이 한곳에서 맞추고, 셋 다 **사용자의
  조작으로 상태가 바뀔 때만** 울리는 `.onChange`로 몬다. **화면이 뜨고 지는
  것(`.onAppear`/`.onDisappear`)에 기대면 안 된다** — 아래 함정 표 참고.

  **새로 좁히는 길을 만들지 말고 이 값에 얹을 것.** 길이 둘이었을 때 한쪽만
  풀려 "전체 해제를 눌렀는데 전부가 안 보인다"가 된 적이 있다.
- `pendingDetailCardID` — 공유로 만든 카드 → 그 카드 상세까지 밀어 넣음.
  **일회성**이라 소비하고 바로 nil로 되돌린다. `.onChange`뿐 아니라
  **`.onAppear`에서도 소비한다** — 갤러리가 홈 오른쪽 칸에 살아서 좁은
  화면에서는 값이 정해진 *뒤에야* 만들어지고, `.onChange`만으로는 그때를
  놓친다. 홈도 이 값을 지켜보다가 오른쪽 칸이 갤러리를 보고 있지 않으면
  옮겨 준다.
- `showCardDetail(_:)` — 위 값을 걸고 첫 탭으로 옮기는 함수.

카테고리 칩은 `AppNavigation`을 거치지 않는다. `HomeView.browse(category:)`가
**범위를 `.all`로 되돌린 뒤** 갤러리의 뷰모델에 필터를 곧장 건다. 두 가지
이유가 겹쳐 이렇게 됐다.

- 칩 목록은 처음부터 모든 카드에서 모은 것이라(`allCategories`), 범위를 그대로
  두면 보드를 보고 나온 뒤에는 그 보드 안에서만 걸러져 칩에 보이던 카드가
  사라진다.
- 범위가 바뀌면 갤러리는 카테고리 필터를 비운다(카테고리를 보고 나와 보드에
  들어갔더니 그 보드 안에서 또 그 카테고리만 걸려 있던 문제). 필터를
  `AppNavigation`의 우편함으로 보내면 범위 변경과 필터 설정이 서로 다른 갱신에
  실려, **비우는 쪽이 방금 건 필터를 도로 지운다.** 같은 갱신 안에서 뷰모델을
  직접 맞추면 그 다툼이 없다.

**다음에 손댈 때**: 위의 "가로지르는 이동"들은 어떤 형태로든 남아야 한다.
각각 실제 사용 흐름의 끝단이다.

그리고 **무엇을 보여 줄지 정하는 값이 둘이라는 것**에 주의할 것.
`navigation.galleryScope`와 `HomeView.sidebarSelection`은 늘 같은 것을
가리켜야 한다. 하나만 옮기면 왼쪽 목록은 보드를 짚고 있는데 오른쪽은 전체를
보여 주는 꼴이 된다 — 카테고리 칩에서 실제로 그렇게 어긋난 적이 있다.

### 1.4 주요 화면 파일

`PlaceCardDetailView`(1029) · `EditPlaceCardSheet`(1553) · `AddPlaceCardView`(620) ·
`PlaceCardListRow`(165) · `PlaceStatusFilterBar`(244) · `OnboardingView`(98) ·
`TrashView`(161) · 각종 시트(`AddBoardSheet`, `EditBoardSheet`,
`CategoryPickerSheet`, `ImportBoardSheet`, `FindDuplicatesSheet`,
`LegalDocumentView`).

색과 치수는 `Views/Theme.swift`에 모여 있다. 화면은 다크로 고정돼 있다
(`PlaceCardsApp`) — 밝은 모드면 `Theme`의 검정 배경 위에 시스템 기본 검정 글씨가
얹혀 글자가 사라진다.

**공유로 들어온 것을 받는 화면 넷**은 §2.1 때문에 따로 취급한다:
`SharedPhotoBoardPickerSheet` · `SharedLinkBoardPickerSheet` ·
`MapScreenshotImportSheet` · `MapLinkImportSheet`.

앞의 둘은 이름에 "BoardPicker"가 남아 있지만 **더 이상 게시판을 묻지 않는다.**
공유로 들어온 카드는 어느 게시판에도 속하지 않은 채 "가져오기"로 간다. 이름을
그대로 둔 것은 `MainTabView`의 공유 표시 장치를 건드리지 않기 위해서다(§2.1).
Google 지도 **목록** 공유만 예외로 묻는데, 그건 "어느 게시판에 넣을까"가 아니라
"만들 게시판 이름이 무엇인가"라서다.

뒤의 둘은 새 카드를 만들지 않는다 — 이미 있는 카드를 채우기만 한다.

**보드 순서는 `Board.sortIndex`에 값으로 적혀 있다.** 처음에는 배열의 자리
그 자체였지만(227차), 그러면 **이 기기 밖으로 나갈 방법이 없어** 239차에
값으로 옮겼다. `sortIndex`는 옵셔널이고 nil은 "차례를 손으로 정한 적 없음"이라
뒤로 가서 만든 순으로 선다 — 한 번도 안 건드린 사용자에게는 예전과 똑같이
보인다.

**배열은 여전히 정렬돼 있어야 한다.** 화면들이 `boards`를 위에서부터 그냥
그리기 때문이다. 배열을 `sortIndex`에 맞춰 두는 자리가 넷이다 — 불러올 때,
저장할 때, 병합한 뒤, 옮긴 뒤(`StorageService`).

`Board.orderedBefore`는 **완전한** 순서다(번호 → 만든 때 → id). 비기는 자리가
남으면 두 기기의 정렬 결과가 갈리므로 마지막에 id가 온다. 그리고 `moveBoards`는
**자리가 실제로 바뀐 게시판에만** 시각을 찍는다 — 전부 찍으면 가만히 있던
게시판까지 "방금 고친 것"이 되어 그사이 다른 기기에서 바꾼 이름을 덮는다.

바꾸는 화면(`BoardReorderSheet`)을 따로 띄우는 것은
왼쪽 목록이 `List(selection:)`이고 **그 선택이 오른쪽 칸을 정하기** 때문이다.
끌어 옮기려면 편집 모드가 필요한데, 편집 모드의 선택은 "여럿 고르기"라 둘이
같은 자리를 놓고 다툰다.

**"가져오기"는 받은 것을 쌓아 두는 곳이지 머무는 곳이 아니다.** 거기 있는 카드가
할 일은 **사용자 보드로 가는 것**이고, 가고 나면 저절로 거기서 빠진다. 보낸 것과
아직 안 보낸 것이 한데 섞이면 무엇이 남았는지 알아보려면 하나하나 열어 봐야 한다.

거는 곳은 `StorageService.save(_:)` 한 군데다. 보내는 길이 여럿이라(카드 화면의
"보드에 추가", 일괄 작업의 추가와 이동) 하나하나에 붙이면 빠뜨린다. 그리고
**속한 상태가 아니라 새로 속하는 순간을 본다** — 상태만 보면 보드를 달고 들어오는
카드(파일로 들여온 게시판, Google Takeout)가 "가져오기"에 한 번도 안 보이고
지나간다. 백업 복원(`merge`)은 `save(_:)`를 타지 않고, 파일로 들여온 게시판은
새 카드라 둘 다 이 규칙에 걸리지 않는다.

**카드가 어느 보드에 속하는지는 `PlaceCardDetailView`의 "보드" 구역에서만
보인다.** 칩의 x는 그 보드에서 빼기일 뿐 삭제가 아니고, 마지막 보드에서 빼도
카드는 "모든 카드"에 남는다 — 그래서 비어 있을 때도 구역을 숨기지 않고 "어느
보드에도 없습니다"를 적는다. 아무 데도 없는 것이 사고가 아니라 정상이라는 것을
말해 주는 자리가 거기뿐이다.

갤러리의 격자 칸과 목록 줄에는 같은 소속이 **읽기 전용 배지**(`BoardBadges`,
`PlaceCardListRow.swift`)로 붙는다. **지금 보고 있는 보드는 뺀다** — 그 보드를
열어 둔 채로 보는 목록에서는 모든 칸에 같은 이름이 붙어 아무것도 구별해 주지
못하면서 자리만 먹는다. 알고 싶은 것은 "여기 말고 또 어디에"다. 이름으로 보여
주는 개수는 격자 하나, 목록 둘이고 넘치면 "+2"로 줄인다. 여기에 x를 두지 않은
것은 일부러다 — 좁은 칸이라 넘기다 잘못 누르기 쉽고, 보드에서 빼기는 되돌리기
화면이 없다.

`GoogleMapWebView`(326) · `NaverMapWebView`(208)는 이름만 View다 — 내용은 §4.1.

---

## 2. 갈아엎어도 반드시 살아남아야 하는 동작

**이 절이 이 문서의 이유다.** 아래는 전부 사용자가 실제로 겪은 버그를 고친
결과다. UI를 다시 쓰면 코드 모양이 사라지면서 같은 버그가 되돌아오기 쉽다.
각 항목은 "무엇이 / 왜 / 어디에".

### 2.1 공유 받기 — 시트 하나, 큐, 그리고 `.sheet(item:)`

Share Extension이 사진이나 링크를 넘기면 `MainTabView`가 받아 화면을 띄운다.
여기에 세 가지 규칙이 있고 전부 버그에서 나왔다.

1. **`.sheet(item:)`이어야 한다.** `.sheet(isPresented:)` + 내용 안에서
   `if let payload`로 하면 **빈 시트가 뜬다.** 표시 시점에 그 옵셔널이 nil이면
   SwiftUI는 구조적으로 빈 시트를 띄우고, 앱을 다시 열면 body가 재평가되며
   제대로 나온다 — "처음엔 비어 있다가 다시 열면 된다"는 그 오래된 증상이
   정확히 이것이었다. 페이로드를 **item 안에** 실으면 빈 시트를 표현할 수 없다.
2. **한 번에 하나, 나머지는 큐로.** 사진과 링크가 같이 도착하면 예전엔 서로
   덮어써서 하나가 조용히 사라졌다. `queuedShares` + `isPresentationScheduled`.
3. **포그라운드가 될 때까지 기다린다.** `presentShortly` — 씬이 활성이 될 때까지
   최대 2초 기다린 뒤 150ms 더 쉬고 띄운다. 전환 도중에 시트를 올리면 안 뜬다.

`id`는 페이로드가 아니라 매번 새로 만든다. 같은 종류를 연달아 두 번 공유해도
`.sheet(item:)`이 서로 다른 항목으로 봐야 한다.

### 2.2 지도 앱에 다녀오는 동안 잃어버리지 않기 (`MapOpenContext`)

사용자가 지도 앱으로 나갔다가 장소를 공유해 돌아오면, **그 공유가 지금 화면을
닫는다.** 화면의 `@State`에 있던 것은 같이 사라진다. 그래서 나가기 직전에
"무엇 때문에 나갔는지"를 기록해 둔다.

- **저장된 카드** — `recordMapOpen(cardID:)`. 돌아온 것을 "그 카드에 추가할까요?"로 제안.
- **아직 저장 안 된 사진들** — `recordMapOpen(photoDatas:)`. 링크로 만들어지는
  카드에 그 사진들을 얹는다. 사진 바이트는 캐시 디렉터리에 파일로 쓴다(지도 앱에
  있는 동안 앱이 종료될 수 있다).

둘은 배타적이다. 한 번의 지도 열기가 양쪽일 수는 없어서, 한쪽을 기록하면 다른
쪽을 지운다. 유효기간 5분.

공유가 돌아왔을 때 뜨는 화면 자리는 **`MainTabView.pendingShare` 하나뿐**이다.
`recordMapOpen(photoDatas:)`로 나간 것은 나가기 직전 화면(`AddPlaceCardView`)이
바로 그 자리를 차지하고 있던 채로 돌아온다 — 지도 앱으로 나가는 것은 화면을
닫지 않고, 그저 다른 앱으로 전환할 뿐이기 때문이다. 그 자리를 비우지 않으면
돌아온 공유는 큐에서 조용히 기다리기만 하고, **사용자가 지금 화면을 손으로
닫아야만** 다음이 뜬다(신고: "닫아야 다시 공유창이 뜨면서 진행이 된다").

`MainTabView.dismissCurrentShareIfResumingMapTrip(recentPhotoDatas:)`가 이걸
스스로 한다. `recentPhotoDatas()`가 비어 있지 않다는 것 자체가 "방금
`AddPlaceCardView`에서 지도로 나갔다가 돌아왔다"는 신호이므로, 그때는 지금 뜬
시트를 그냥 내린다 — 잃을 게 없다. 그 화면이 들고 있던 사진은 이미 여기에
저장돼 있고, 곧 뜰 새 화면도 같은 사진으로 다시 열린다.

> **개편 시 가장 깨지기 쉬운 곳.** `AddPlaceCardView`가 지도 앱으로 나가는
> 입구는 **여섯 군데**다(사진 GPS 메뉴 2 + 행 단위 "지도에서 찾기" 메뉴 4).
> 처음엔 둘만 기록해서 나머지 넷에서 똑같이 사진을 잃었다(PR #67).
> 지금은 전부 `leavingForMapApp()` 하나를 거친다. **새 UI에서도 지도 앱으로
> 나가는 모든 버튼은 예외 없이 그 함수를 거쳐야 한다.**

### 2.3 저장은 "지금 화면에 있는 것"을 쓴다

`viewModel.createCards(images:source:)`는 사진을 **인자로 받는다.** 예전엔 뷰모델의
`selectedImages`를 읽었는데 그건 AI 분석 때만 채워졌다. 결과:

- 분석을 안 누르면(공유 링크가 이미 장소를 확정해 주면 누를 이유가 없다)
  **썸네일이 화면에 보이는데 카드에는 한 장도 안 담겼다.**
- 분석 후 사진을 더하거나 빼면 **옛날 묶음이 저장됐다.**

**개편 시**: 저장 직전에 화면의 현재 상태를 넘기는 이 모양을 유지할 것.
"어느 시점의 사본"을 들고 있으면 같은 버그가 다시 생긴다.

### 2.4 사진 썸네일의 두 컨트롤

카드 상세의 사진 줄은 썸네일마다 **x(삭제, 오른쪽 위)** 와 **★(대표사진, 왼쪽 아래)**.
사진 자체를 누르면 전체화면.

- `ZStack` 형제가 아니라 **`.overlay(alignment:)`**. 예전에 별을 `ZStack`에 두고
  `.frame(maxWidth: .infinity, alignment: .bottomLeading)`으로 구석에 붙였더니,
  그 frame이 **버튼 자신의 것**이라 탭 영역이 셀 전체로 퍼져 사진과 x를 덮었다.
- `.contentShape(Rectangle())` 없으면 `.padding`이 탭을 안 받고 글리프만 받는다.
- 삭제에는 확인 대화상자를 **남겨 뒀다.** 가로 스크롤되는 저장된 사진 줄이라
  잘못 닿으면 되돌릴 데가 없다. (고르는 중인 사진 줄에는 확인이 없다 — 아직
  저장 전이라 잃을 게 없다.)

### 2.5 대표사진 기본 순서

`PlaceCard.coverPhoto`: 명시적 지정 → **사용자 사진**(`onsitePhotos` →
`receivedPhotos`) → Google 사진 → 아무거나.

Google 사진을 먼저 보던 시절이 있었고, 그때는 "카드에 사진이 없을 때만 Google
사진을 받는다"는 전제라 무해했다. 지금은 둘 다 받으므로, 그 순서를 되살리면
**새로고침 한 번에 직접 찍은 사진이 스톡 사진으로 조용히 바뀐다.**

### 2.6 백업·복원의 병합 규칙

복원은 **덮어쓰기가 아니다.** 카드마다:

| 상태 | 동작 |
|---|---|
| 여기 없음 | 추가 |
| 있고, 백업의 `updatedAt`이 더 나중 | 백업 것으로 교체 |
| 있고, 같거나 더 이전 | 그대로 |
| **백업에 아예 없음** | **그대로** |

`StorageService.merge`. `save(_:)`를 거치지 않고 넣는다 — 그 함수는 `updatedAt`을
지금으로 찍어서 복원된 카드를 전부 "방금 수정"으로 만들고, 다음 복원의 비교를
망가뜨린다. 미디어 파일은 **하나도 지우지 않는다.**

게시판도 같은 규칙이다(238차). `Board.updatedAt`이 **옵셔널인 것이 중요하다** —
필수로 두면 이 필드가 생기기 전에 저장된 게시판이 전부 디코딩에 실패한다(§3).
한 번도 안 고친 게시판은 `changedAt`이 `createdAt`으로 답한다.

**백업 한 벌은 파일이 아니라 폴더다**(255차). `metadata.json` 하나와 `photos/`
아래 사진 파일들. 예전에는 사진을 base64로 JSON 안에 인라인했는데, 그러면
인코딩할 때 사진이 **세 벌로** 메모리에 올라간다 — 원본 사전, base64 문자열,
직렬화된 출력. 328장/400MB에서 최고점이 2GB에 닿아 앱이 `EXC_RESOURCE`로
죽었다. 지금은 `FileManager.copyItem`으로 한 장씩 파일에서 파일로 옮기므로
**바이트가 메모리를 거치지 않는다.**

건드릴 때 지켜야 할 것 셋:

- **옛 `.json` 백업도 계속 읽어야 한다.** 이 변경 전에 만든 것이 사용자
  폴더와 iCloud에 남아 있다. `.json`이면 옛 형식, 확장자가 없으면 폴더
  형식으로 가른다 — **이름으로** 가르는 이유는 아직 안 내려온 것은 어느 쪽도
  디스크에 없어 내용으로 못 가리기 때문이다.
- **사진 목록은 `metadata.json`에서 얻는다.** 디렉터리 목록에는 아직 안 내려온
  파일이 플레이스홀더 이름으로 나오고, 그 이름 규칙을 되돌리는 것은 추측이다
  (CLAUDE.md §4). 되돌아오는 쪽(`writePhotos`)도 점으로 시작하는 이름을
  건너뛴다 — 안 거르면 `.icloud` 껍데기가 사진인 양 복사된다.
- **복원 후보를 한꺼번에 디코드하지 않는다.** `loadRestorableBackups()`가 그랬고,
  기기 셋이면 세 기기의 사진 전체가 동시에 메모리에 있었다. 지금은
  `restoreAll(into:)`이 한 벌씩 처리한다.

iCloud 자동 복원은 **현지가 완전히 빈 경우에만** 돈다. 이 조건을 풀면 지운
카드가 다음 실행 때 되살아난다.

**이 규칙은 기기 간 동기화도 그대로 쓴다**(`CloudSyncService`, 234차). 새로 쓰지
않은 것은 일부러다 — 여기서는 컴파일조차 확인할 수 없으므로(CLAUDE.md §1) 이미
쓰이는 규칙을 재사용하는 쪽이 안전하다. 동기화 쪽에만 있는 규칙이 둘 있다.

- **먼저 받아 병합하고 그 다음에 올린다.** 올리기는 `savePolicy = .allKeys`,
  즉 이 기기 것으로 무조건 덮는다. 덮어쓰기가 안전한 건 직전에 받아 병합해
  두었기 때문이고, **받기가 실패하면 올리지 않고 멈춘다.**
- **묘비는 병합보다 먼저 적용한다**(236차). 반대로 하면 방금 지운 것으로 들어온
  카드를 병합이 한 번 되살렸다가 곧바로 다시 지우고, 그사이 사진까지 받는다.
  `merge` 자체는 안 고쳤다 — 백업 복원에서는 지웠던 카드가 돌아오는 것이 오히려
  사용자가 바라는 일이다. 거르는 것은 동기화 쪽에서만 한다.

### 2.7 앱을 닫을 때

`scenePhase == .background`에서 폴더 백업과 iCloud 스냅샷을 **UIKit 백그라운드
작업 어서션 안에서** 돌린다. 어서션이 없으면 iOS가 몇 초 만에 프로세스를
정지시키는데, 백업은 라이브러리의 모든 사진을 읽어 인코딩한다 — 사진이 쌓인
기기에서만 조용히 실패한다.

### 2.8 기타 UI 동작 중 근거가 있는 것

- **갤러리 레이아웃**(격자/목록)은 `@AppStorage`. 화면을 떠날 때마다 초기화되면
  안 된다. 지금은 **범위마다 따로** 기억한다(보드 하나하나, 모든 카드, 가져오기)
  — 사전 하나를 JSON으로 담는다. `@AppStorage`는 키가 고정이라 범위마다 다른
  키를 쓸 수 없고, `UserDefaults`를 직접 읽으면 값이 바뀌어도 화면이 다시 그려지지
  않는다. 예전 전역 키(`galleryLayout`)는 범위별 값이 없을 때의 기본으로 남겨 둔다.
- **크레딧 줄**(`CreditFooter`)은 왼쪽 목록·갤러리·설정 **맨 아래**. 갤러리는
  레이아웃이 둘이라 **양쪽 다** 넣어야 한다.
- **게시판 삭제**는 카드가 없을 때만 스와이프로 제안한다. 카드를 조용히 데려가지
  않기 위해서다.
- **카드 셀을 `NavigationLink`로 감싸지 않는다.** 셀 전체를 탭 대상으로 가져가
  셀 안의 즐겨찾기·방문·전화·지도 버튼을 삼킨다. `.onTapGesture`를 쓴다.

### 2.9 사진의 촬영 날짜를 방문 날짜로 (`PhotoVisitDates`)

**PR 넷과 사용자 신고 셋을 거쳐 자리 잡았다**(227·241·242·243차). 규칙 하나를
되돌리면 그중 하나가 그대로 되살아나므로 손대기 전에 이 넷을 다 읽을 것.

- **`onsitePhotos`만 본다.** 지도 스크린샷(`mapScreenshots`)·받은 사진
  (`receivedPhotos`)·Google 공식 사진(`officialPhotos`)은 "내가 거기 있었다"의
  근거가 못 된다. 아래 두 규칙이 느슨한 것은 **이 배열만 보기 때문에** 성립한다.
- **촬영 날짜는 꼭 있어야 하고, 위치는 있으면 견주고 없으면 넘어간다**(242차).
  위치는 **반증하는 데** 쓰는 것이지 입증에 필요한 것이 아니다. 둘 다 요구하면
  위치 정보를 꺼 두고 찍은 사진이 날짜가 뻔히 있어도 전부 버려진다.
- **기준점은 확정된 좌표만**(`isPlaceConfirmed`, 243차). 확정 전 좌표는
  지도 공유 링크에서 뽑은 어림값이라 실제 가게 자리가 아닐 수 있다. **추정치로
  실측 GPS를 반증하면 앞뒤가 바뀐다.** 확정 전에는 현장 사진 중 GPS가 있는 첫
  장을 기준으로 사진들끼리 견준다 — "한 자리에서 찍힌 사진 묶음"이라는 조건은
  그대로고, 그 자리가 어디인지를 추정치에게 묻지 않을 뿐이다.
- **못 찾았으면 이유를 말한다**(`reasonNothingFound(for:)`, 242차). 한 줄로
  뭉뚱그리면 사용자가 할 수 있는 일이 경우마다 다른데 구별이 안 되어 "기능이
  안 된다"로만 보인다. 검사 순서는 `candidates`가 걸러 내는 순서와 같아야 한다 —
  어긋나면 여기서 하는 말과 저기서 하는 일이 달라진다.
- **`body`에서 부르지 말 것.** 값이 저장돼 있지 않은 옛 사진에 대해 파일을 열어
  EXIF를 읽으므로 스크롤할 때마다 디스크를 두드린다. `.task(id:)`로 몬다.

그리고 **저장된 파일에는 EXIF가 없다.** `MediaStore.saveImage(_ image:)`가
UIImage로 다시 인코딩하면서 찍힌 때도 자리도 사라진다. 그래서 원본 바이트가
아직 손에 있는 자리에서 읽어 `MediaItem`에 얹어 둔다
(`capturedAt`·`capturedCoordinates`, 둘 다 옵셔널).

---

## 3. UI와 그 밖의 경계

```
Views/         화면. 9,515줄
ViewModels/    경계. 화면당 하나씩 붙어 있지만 로직은 여기 있다. 1,603줄
Models/        저장 포맷. 820줄
Services/      동작. 7,151줄
```

이 절은 원래 "Models와 Services는 건드리지 말 것"이라고 적고 있었다. UI만
바꾸는 작업을 전제한 말이었다. 실제로는 개편 중에 둘 다 건드려야 했다 — 카드가
여러 보드에 속하는 것도, 삭제를 되돌리는 것도 UI로는 만들 수 없다. 그러니
"건드리지 말 것"이 아니라 **건드릴 때 무엇이 깨지는지**를 적는다.

**`Models/`는 저장 포맷이다.** `PlaceCard`·`Board`·`MediaItem`·`SourceType`은
디스크의 JSON과 1:1이다.

- **필드를 지우거나 이름을 바꾸면** 저장된 카드가 전부 디코딩에 실패한다.
  특히 **`SourceType`의 case는 절대 지우지 않는다**(CLAUDE.md §4).
- **더하는 것은 안전하다.** 옵셔널로 더하면 예전 카드는 nil로 디코딩된다.
  `boardIds`·`deletedAt`·`isImported`가 그렇게 들어갔고, 이관 작업이 필요 없었다.
- 뜻이 바뀌는 필드는 **옛 필드를 남기고 새 필드를 더한다.** `boardId`는 카드가
  보드 하나에만 속하던 시절의 것인데, 지우지 않고 `boardIDs`의 첫 칸과 발을
  맞춰 둔다 — 여기서 쓴 백업을 예전 빌드에서 열어도 카드가 제 보드에 들어간다.

**`Services/`는 동작이다.** 공유 파싱, 지도 열기, 검색, 백업, 저장, 번역.

여기를 바꿀 때 가장 위험한 것은 **같은 가정이 여러 곳에 흩어져 있는 경우**다.
"카드는 보드 하나에 속한다"를 고칠 때 `placeCards`를 읽는 스물한 곳을 전부
훑어야 했고, 그중 셋에 진짜 버그가 숨어 있었다(`deleteBoard`가 남의 보드 카드를
같이 지움, `importBoard`가 카드를 복제함, `search`가 지운 카드를 계속 내놓음).
한 군데라도 빠뜨리면 **화면 하나에서만** 이상하게 보인다 — 가장 찾기 어려운 모양이다.

**`ViewModels/`가 실제 경계다.** `PlaceCardViewModel`이 1,193줄로 가장 크고,
화면이 아니라 "장소 추가 흐름" 전체를 들고 있다. UI를 다시 쓸 때 이걸 같이
갈아엎으면 §2의 절반이 날아간다. **먼저 쓰던 그대로 붙여 보고, 필요한 것만
나중에 손대는 쪽이 안전하다.** 개편 중에 실제로 그렇게 했고, 이 뷰모델에 손댄
것은 "게시판 없이도 카드를 만들 수 있게" 한 것 하나뿐이다.

---

## 4. UI로는 못 바꾸는 것

### 4.1 Naver 지도 말풍선은 이 저장소에 없다

`NaverMapWebView`는 WKWebView를 아래 페이지로 보내기만 한다. 마커·말풍선·버튼을
그리는 코드는 전부 그 페이지 안이다:

```
mrnoh99/Peragra  web/public/naver-map-embed.html
```

그리고 **Peragra 저장소는 수정 금지**(CLAUDE.md §5, 사용자 지시 2026-09-17).
즉 **Naver 말풍선의 모양은 지금 바꿀 수 없다.** 사본을 떠서 우회하지 마라 —
한 번 갈라놨다가 되돌린 적이 있다(189차 → 190차).

바꿀 수 있는 것은 Swift가 그 페이지로 **보내는 값**뿐이다
(`NaverMapWebView.MarkerPlace`, `LocalizedStrings`).

**결과: 세 지도 탭의 말풍선을 함께 바꾸는 작업은 불가능하다.** Apple 탭
(`PlacesMapView.appleMapAnnotation`)과 Google 탭(`GoogleMapWebView`의 HTML)은
이 저장소 안이라 바꿀 수 있지만, 셋은 2026-09-17 기준 일부러 같은 모양으로
맞춰 놓은 것이라 한쪽만 고치면 그 정합성이 깨진다.

### 4.2 그 밖

- **번역**: 화면에 보이는 한국어는 전부 `.localized`, 대응은 `Localization.swift`.
  고쳤으면 `python3 Tools/localization/check.py`를 **반드시** 돌린다.
- **새 파일**: 이 프로젝트는 Xcode 자동 동기화 그룹을 쓰지 않는다. 새 `.swift`는
  `project.pbxproj`에 네 자리(빌드 파일·파일 참조·그룹·소스 단계)를 직접 넣어야
  한다. `AppCredit.swift`가 최근 사례다.
- **CI가 유일한 컴파일 검증**이다(CLAUDE.md §1). 개발 환경에 Xcode도 Swift도 없다.
- **기기 간 동기화는 단계로 간다.** iCloud에 쓰는 것이 둘이 됐다 — 기존
  `CloudBackupService`(문서 컨테이너에 JSON 스냅샷, 기기마다 제 파일)와 새
  `CloudSyncService`(CloudKit). 지금 CloudKit 쪽은 **주고받기까지** 왔다 —
  설정의 "지금 맞추기"가 사용자 지정 존 `PlaceCards`에서 **먼저 받아 병합하고
  그 다음에 올리고, 끝으로 사진을 주고받는다.** 저장은 여전히 `StorageService`의 JSON이 전부다(CloudKit은
  그 위에 얹힌 것이지 대체가 아니다). 동기화는 되돌리기 어렵고 CI로는 거의
  검증되지 않으므로(실기기 두 대가 필요), 붙이는 순서를 연결 확인 → 올리기 →
  내려받아 병합 → 사진 → **삭제 전파** → 기존 저장 정리로 나눴다.

  **순서가 이 기능의 전부다.** 올리기는 `savePolicy = .allKeys`, 즉 이 기기
  것으로 무조건 덮는다. 기본값(`.ifServerRecordUnchanged`)을 쓸 수 없어서다 —
  새로 만든 `CKRecord`에는 서버가 준 변경 태그가 없어 이미 있는 레코드마다
  충돌로 실패한다. 덮어쓰기가 안전한 것은 **올리기 직전에 받아 병합해 두었기
  때문**이고, 그래서 받기가 실패하면 올리지 않고 멈춘다. 이 순서를 뒤집거나
  받기 실패를 흘려보내는 것이 여기서 자료를 잃는 유일한 길이다.

  같은 이유로 "못 받은 것"과 "받을 게 없던 것"을 반드시 구별한다. 셋 다 막아
  두었다: 존 가져오기 실패(`recordZoneFetchResultBlock`의 `.failure`)는 던지고,
  `moreComing`이 끝나지 않으면 던지고, **레코드 하나가 실패하거나 이 빌드가
  디코딩하지 못하면 그 id를 올릴 때 뺀다.** 마지막 것이 제일 미묘하다 — 다른
  기기가 먼저 새 버전으로 올라가면 이 빌드가 못 읽는 레코드가 생기는데, 그걸
  건너뛴 채 같은 id의 옛 카드를 올리면 멀쩡하던 최신 내용이 옛 것으로 덮인다.

  병합 규칙은 새로 쓰지 않고 `StorageService.merge`를 그대로 쓴다 — 백업
  복원이 쓰는 그 규칙이다(없으면 추가, `updatedAt`이 더 나중이면 교체, 없는
  것은 그대로). 여기서 컴파일조차 확인할 수 없으므로 이미 쓰이는 규칙을
  재사용하는 쪽이 새로 쓰는 것보다 안전하다.

  카드 하나는 통째로 JSON `payload` 한 필드에 들어간다. 필드를 펼쳐 두면
  `PlaceCard`에 필드가 붙을 때마다 CloudKit 스키마와 짝을 맞춰야 하고,
  빠뜨리면 그 필드만 조용히 사라진다.

  질의(`CKQueryOperation`)가 아니라 `CKFetchRecordZoneChangesOperation`으로
  받는다. 질의는 대시보드에서 **queryable 색인**을 걸어야 도는데 자동 생성된
  스키마에는 그게 안 붙는다 — 코드가 맞아도 "invalid query" 하나로 끝난다.

  **사진은 존이 따로다**(`PlaceCardsPhotos`). 카드 존은 동기화할 때마다 통째로
  받는데 사진이 같은 존에 있으면 그때마다 사진까지 전부 내려받기 때문이다.
  존이 나뉘어 있으면 사진 쪽은 `desiredKeys = []`로 **이름만** 받아 무엇이 이미
  올라가 있는지 보고, 없는 쪽으로만 주고받는다. 파일 이름(`UUID().jpg`)이 곧
  레코드 이름이라 이름 견주기만으로 정해지고, 한 번 오간 사진은 다시 오가지
  않는다. 카드가 안 가리키는 파일은 올리지 않는다.

  사진이 실패해도 카드는 이미 끝난 상태다(병합은 디스크에 쓰였고 올리기도 됐다).
  그래서 사진 실패를 통째로 실패처럼 보이지 않게 하고 사진 쪽만 안 됐다고 말한다
  — 다음 동기화 때 이름으로 견주어 다시 대상이 된다.

  **아주 지운 카드에는 묘비를 남긴다**(`StorageService.purgedCardIDs`). 안 그러면
  `purge`가 배열에서 카드를 없애 버려 "지웠다"는 사실이 어디에도 안 남고,
  클라우드에 남은 그 카드가 다음에 받을 때 "이 기기에 없는 카드"로 보여 **그대로
  되살아난다.** 지우는 길 넷이 전부 그랬다 — 휴지통의 영구 삭제, 휴지통 비우기,
  기한 지난 것 자동 정리(`purgeExpiredTrash`, 앱이 뜰 때마다 돈다), 중복 카드
  합치기. 자동 정리는 앱을 켤 때마다 지우고 동기화할 때마다 되살아났다.

  묘비는 `UserDefaults`에 둔다 — 사용자 자료가 아니라 동기화 장부이고, 세대
  번호로 순서를 지키는 파일 쓰기 경로를 하나 더 늘리지 않는 편이 안전하다.
  받을 때 **병합보다 먼저** 적용하고(반대로 하면 되살렸다가 다시 지우며 그사이
  사진까지 받는다), 묘비가 있는 카드는 병합에 아예 넘기지 않는다. `merge`
  자체는 안 고쳤다 — 백업 복원도 그걸 쓰는데 거기서는 지웠던 카드가 돌아오는
  것이 오히려 사용자가 바라는 일이다.

  **사진 삭제는 따로 할 일이 없다.** `deletePhoto`가 카드에서 그 사진을 빼고
  `save`로 `updatedAt`을 올리므로, 카드가 통째로 오가면서 다른 기기의 카드에서도
  사라진다. 남는 것은 서버의 `PhotoAsset` 레코드뿐이고(고아), 그건 아직 안 치운다.

  **단추는 둘, 서비스는 하나다.** 설정의 "지금 맞추기"와 갤러리(게시판 목록)
  툴바의 동그란 화살표가 같은 `CloudSyncService`를 누른다. 앱 루트
  (`PlaceCardsApp`)가 하나 만들어 `environmentObject`로 내려보내므로, 한쪽이
  도는 동안 다른 쪽 단추도 같이 돌고 두 번 시작되지 않는다 — `syncNow`의
  `isBusy` 빗장은 **같은 인스턴스 안에서만** 듣기 때문에, 화면마다 따로
  만들면 그 빗장이 무력해진다.

  끝났다고 늘 알리지는 않는다(`SyncState.deservesNotice`). 받고·바뀌고·
  지워진 것이 하나도 없으면 단추가 돌았다가 멎는 것으로 끝낸다. 자주 누르는
  단추에 매번 알림을 띄우면 그것부터 닫는 버릇이 들어 정작 바뀐 날을 놓친다.
  올린 수는 세지 않는다 — 누를 때마다 일어나는 일이라 새 소식이 아니다.

  **게시판 이름 변경도 옮겨 간다.** `Board`에 `updatedAt`(옵셔널)을 더하고
  `saveBoard`가 찍는다. 옵셔널인 것이 중요하다 — 필수로 두면 이 필드가 생기기
  전에 저장된 게시판이 전부 디코딩에 실패한다. 한 번도 안 고친 게시판은
  `changedAt`이 `createdAt`으로 답한다. `merge`는 이제 게시판도 카드와 같은
  규칙으로 다룬다(없으면 더하고, `changedAt`이 더 나중이면 바꾼다).

  **게시판 차례와 삭제도 옮겨 간다.** 차례는 `Board.sortIndex`(옵셔널)에 값으로
  들어간다 — 예전에는 배열의 자리가 곧 차례라 이 기기 밖으로 나갈 방법이
  없었다. `moveBoards`가 옮긴 뒤 번호를 다시 매기는데, **자리가 실제로 바뀐
  게시판에만** 시각을 찍는다. 전부 찍으면 가만히 있던 게시판까지 "방금 고친
  것"이 되어 그사이 다른 기기에서 바꾼 이름을 덮는다.

  `Board.orderedBefore`는 반드시 **완전한** 순서여야 한다(번호 → 만든 때 → id).
  비기는 자리가 남으면 두 기기의 정렬 결과가 갈린다. nil은 "차례를 손으로 정한
  적 없음"이라 뒤로 가고 만든 순으로 서므로, 차례를 한 번도 안 건드린 사용자에게는
  예전과 똑같이 보인다.

  게시판 삭제는 카드와 같은 묘비다(`purgedBoardIDs`, `PurgedBoardDoc`). 카드
  묘비와 **통을 나눈다** — 한 통에 섞으면 id만 보고 어느 쪽을 지우라는 것인지
  알 수 없다. 받아서 적용할 때는 `deleteBoard`를 그대로 태우므로, 그 게시판에
  들어 있던 카드에서 이 게시판만 빠지는 처리까지 똑같이 일어난다(카드는 안
  지워진다).

  변경 토큰은 일부러 저장하지 않는다. 매번 전부 받아 느리지만, 토큰을 잘못
  들고 있으면 받아야 할 것을 **조용히** 건너뛴다 — 두 기기가 실제로 맞는지
  확인되기 전에는 느린 쪽이 맞다.
  `PlaceCards.entitlements`의 `icloud-services`에 `CloudKit`이 들어갔고,
  **Xcode에서 iCloud 기능의 CloudKit 체크도 해야 한다** — 안 하면 프로비저닝
  프로파일에 안 들어가 조용히 실패한다(App Group과 같은 종류의 함정).
- **사진에서 가져오는 방문 날짜는 "카드 좌표"에 매여 있지 않다.**
  `PhotoVisitDates.candidates`는 현장 사진의 촬영 날짜를 방문 기록으로 삼는데,
  그 사진이 "그 장소에서" 찍혔는지 견줄 기준점이 필요하다. 예전에는 그 기준점이
  카드 좌표뿐이라 **좌표가 없으면 바로 빈 손으로 돌아왔다.** 그런데 사진을
  공유해 만든 카드에는 좌표가 *아직* 없다 — 지도에서 장소를 확정해야 생긴다.
  그래서 "사진 날짜가 구글에서 확정한 뒤에야 나타난다"가 됐고, 상세 화면의
  "사진에서 방문일 찾기" 단추까지 그때는 비활성이라 손으로 부를 수도 없었다.

  지금은 `PhotoVisitDates.reference`가 **확정된 좌표만**(`isPlaceConfirmed`)
  기준으로 삼고, 그 밖에는 **현장 사진 중 GPS가 있는 첫 장**을 기준점으로 세운다.

  확정 전 좌표를 기준에서 뺀 것이 핵심이다. 그건 **어림값**이다 — 지도 앱이
  공유해 준 링크에서 뽑았거나, 주소 텍스트를 지오코딩했거나, 사진 GPS를 옮겨
  놓은 것. 앞의 둘은 가게가 실제로 있는 자리가 아니라 지도 화면의 중심이거나
  그 근처일 수 있고 수백 미터가 예사다. 반면 사진의 GPS는 기기가 그 자리에서
  잰 **실측**이다.

  어림값으로 실측을 반증하면 앞뒤가 바뀐다. 실제로 그 탓에 "GPS로 촬영위치찾기로
  지도를 열어 장소를 공유해 만든 카드는 날짜가 안 들어오는데, 구글에서 검색해
  저장하면 그때서야 들어온다"가 됐다. 확정하는 순간 좌표가 진짜 값으로 바뀌면서
  같은 사진이 200m 안으로 들어왔던 것이다. 견주는 일을 없앤 것이 아니라 기준점을 사진 쪽에서
  잡을 뿐이다 — GPS 없는 사진은 여전히 한 장도 통과 못 하고(지도 스크린샷·웹에서
  받은 사진이 대개 그렇다), 200m 검사도 그대로라 첫 장에서 먼 사진은 떨어진다.
  "한 자리에서 찍힌 사진 묶음"이라는 조건은 그대로고, 그 자리가 어디인지를
  구글에게 묻지 않을 뿐이다.

  **그리고 위치는 이제 있어도 되고 없어도 된다.** 예전에는 날짜와 위치가 둘 다
  있어야만 받아들였는데, 위치 정보가 꺼진 채로 찍은 사진은 날짜가 뻔히 있어도
  전부 버려졌다 — 사용자에게는 "날짜가 적혀 있는데 못 찾는다"로 보인다. 위치는
  **반증하는 데** 쓰는 것이지 입증에 필요한 것이 아니다: 다른 동네에서 찍힌
  것이 드러나면 떨어뜨리고, 아무 말이 없으면 사용자가 제 현장 사진으로 붙였다는
  사실을 믿는다. 그 믿음이 성립하는 것은 `onsitePhotos`만 보기 때문이다 —
  지도 스크린샷·받은 사진·Google 사진은 각각 다른 배열로 들어간다.

  그래도 **못 찾는 경우는 남는다.** 그때 `PhotoVisitDates.reasonNothingFound`가
  왜인지 한 줄로 답한다 — 위치 정보가 없는지, 날짜가 없는지, 멀리서 찍혔는지,
  이미 다 적혀 있는지. 예전에는 넷을 "촬영 날짜를 찾지 못했습니다" 하나가 다
  덮어서, 사용자가 할 수 있는 일(카메라 위치 접근 켜기 같은)이 보이지 않고
  그냥 "기능이 안 된다"로 보였다. 제일 흔한 경우가 **위치 정보가 꺼진 채로
  찍은 사진**인데, 그건 앱이 고칠 수 있는 것이 아니다.

- **사진 앱의 특정 사진을 여는 길은 없다.** iOS에는 사진 하나를 지정해 사진 앱을
  띄우는 공개 API가 없다 — 떠도는 `photos:`·`photos-navigation:` 스킴은
  리버스 엔지니어링된 것이고 특정 사진 지정은 동작이 확인되지 않았다(CLAUDE.md
  §4의 "스킴은 추측하지 않는다"에 걸린다). 그래서 대신 사진 앱에서 **찾을 수
  있게** 해 둔다: `PhotoLibraryAlbum`이 카드의 사진을 라이브러리의 "PinSpots"
  앨범에 담는다.

  **그런데 포기했던 목표가 결국 이뤄진다.** 담기면 라이브러리에 사본이 한 장
  더 생기는데(앱이 가진 것은 줄여 놓은 사본이다), 사진 앱의 **중복 항목**
  (앨범 → 유틸리티)에서 **병합**하면 둘이 하나로 합쳐지면서 화질 좋은 쪽이
  남고 앨범 소속은 양쪽 것을 다 물려받는다 — 즉 **원본이 PinSpots 앨범에
  남는다.** 사본이 생기는 것이 막다른 골목이 아니라 원본으로 가는 길목이다.
  사용자가 실기기에서 확인해 준 동작이고, 이 앱이 부르는 API가 아니라 사진
  앱의 기능이므로 코드로 보장할 것은 없다. 담기 확인창 두 곳에서 이 길을
  가리켜 준다. 앱이 제 안에 가진 사진 파일로 만들기 때문에 예전에 추가한
  사진에도, 공유로 들어온 사진에도 된다 — 대신 라이브러리에서 골라 온 사진은
  한 장 더 생긴다.

- **"장소확정"은 이제 넷이다** — Google Places, Naver 지역 검색에 Apple
  `MKLocalSearch`와 카카오맵을 더했다. 둘의 방식이 서로 다르다:

  - **Apple**은 Google/Naver와 똑같은 "검색해서 고르는" 흐름
    (`AppleLocalSearchService`)이다. `MKLocalSearch`는 애플 기기 내장
    프레임워크라 API 키가 필요 없다 — `EditPlaceCardSheet.confirmPlace()`가
    Google → Naver → Apple 순으로 시도하고, Apple은 키가 없어도 늘
    시도된다. `PlaceSearchResult.isFromGooglePlaces: Bool`은 세 번째
    제공자를 담을 수 없어 `provider: PlaceSearchProvider`(google/naver/
    apple)로 바꿨다 — 이런 종류의 이진 플래그는 나중에 셋째가 생기면
    막힌다는 게 여기서 또 확인됐다.

    **`MKLocalSearch`는 구글처럼 너그럽지 않다.** 두 가지를 겪었다(사용자
    신고: 화면에 "The operation couldn't be completed. (MKErrorDomain error
    4.)").

    - **결과가 없을 때도 오류를 던진다**(`MKError.placemarkNotFound`, 그
      4번이다). 고장이 아니라 답이므로 `run`이 빈 결과로 삼킨다. 안 그러면
      애플이 만든 영어 문장이 한국어 화면에 그대로 뜬다.
    - **`naturalLanguageQuery`는 글자 그대로 맞춰 보는 쪽에 가깝다.**
      호출부가 넘기는 "이름 + 주소"는 층수까지 붙어 있어서(`... 세지로234번길
      5 1층`) 통째로 어긋난다. 그래서 빈 손이면 **이름만으로 한 번 더**
      묻는다(`fallbackQuery`). 엉뚱한 동네가 끼어드는 것은 `coordinateHint`가
      막는다.

    남는 `MKError`도 원문을 보여 주지 않는다 — `message(for:)`가 한 줄로
    바꾸고, 모르는 코드면 `nil`을 돌려 호출부의 일반 문구를 쓴다.
  - **카카오는 검색-선택 흐름이 아예 없다.** 카카오 로컬 API(키워드 검색)를
    쓰면 결과를 저장하는 게 그 API 약관에 걸린다(§4 참고, 이미 내린
    결정). 대신 **공유 링크 자체가 확정이다** — `KakaoPlaceLinkResolver`가
    그 장소 전용 페이지(`place.map.kakao.com/{id}`)를 직접 읽어 낸 것이므로,
    여러 후보 중 하나를 고른 게 아니라 이미 그 장소의 공식 페이지다. 같은
    페이지의 `og:image`가 그 장소의 실제 대표 사진(확인: 800×400 JPEG)을
    들고 있어서 사진도 한 장 딸려 온다.

  `PlaceCard`에 `appleVerified`/`kakaoVerified`(둘 다 옵셔널)를 더하고
  `isPlaceConfirmed`가 넷을 다 본다. `merge(with:)`도 googlePlaceId/
  naverVerified와 같은 규칙으로 둘을 따라간다.

- **"지도에서 열기"가 미검증 카드를 엉뚱한 곳으로 보내던 것 (254차)**:
  Naver `nmap://place`·Kakao `/link/to/`는 `name`을 라벨이 아니라 그
  지도 자체 POI와 매칭할 검색어로 쓴다. 위 `appleVerified`/
  `kakaoVerified`를 붙였을 때 정작 `MapOpeners.swift`가 이 플래그를
  하나도 안 봐서, Google로만 확정된 카드나 AI 추측 이름 + 사진 GPS뿐인
  카드를 Naver/Kakao로 열면 좌표 근처 다른 업체로 잘못 매칭될 수 있었다.
  이제 해당 플래그가 `true`일 때만 정확한 위치 링크를 쓰고 아니면
  검색 링크로 내려간다 — `googlePlaceId`가 있는 카드는 Google에
  `query_place_id`도 같이 보낸다. 자세한 내용은 `CHANGELOG.md` 254차.

---

## 5. 실제로 밟은 SwiftUI 함정

전부 이 코드베이스에서 CI 또는 사용자 제보로 드러난 것이다.

| 증상 | 원인 |
|---|---|
| 시트가 비어서 뜬다 | `.sheet(isPresented:)` + 내용에서 `if let` → `.sheet(item:)`으로 |
| 버튼이 안 눌린다 / 엉뚱한 게 눌린다 | 버튼에 `.frame(maxWidth: .infinity)`를 걸어 구석에 붙임. 그 frame은 버튼 자신의 탭 영역이다 → `.overlay(alignment:)` |
| `.padding`이 탭을 안 받는다 | `.contentShape(Rectangle())` 누락 |
| 셀 안의 작은 버튼들이 안 눌린다 | 셀을 `NavigationLink`/`Button`으로 감쌈 → `.onTapGesture` |
| `unable to type-check this expression in reasonable time` | `.localized` 문자열을 `+`로 길게 이음 → 문장을 쪼개 변수로 |
| `Cannot find type 'MapCameraPosition'` | `import SwiftUI` 누락 (MapKit만으로 부족) |
| `Covariant 'Self' ... stored property initializer` | 저장 프로퍼티 기본값에서 `Self.` 참조 |
| 트레일링 클로저가 엉뚱한 파라미터에 | 기본값 있는 클로저 파라미터를 뒤에 추가. 라벨 없는 트레일링 클로저는 **선언상 마지막** 클로저에 붙는다 |
| `Conflicting arguments to generic parameter 'Result'` | 값을 반환하는 함수를 `withAnimation { }`의 단일 표현식으로 호출. `@discardableResult`로 안 막힌다 |
| `expected key expression in dictionary literal` | `Localization.swift`에서 값이 다음 줄에 있는 항목의 key와 value 사이에 끼워 넣음 → `Tools/localization/check.py` |
| 탭해도 아무 일이 없다 (길게 누르기는 됨) | 같은 뷰에 `.onTapGesture`와 `.onLongPressGesture`를 달았다. SwiftUI는 **나중에 단 제스처에 우선권**을 주므로 뒤에 달린 길게 누르기가 탭을 삼킨다. 순서를 바꾸면 반대로 길게 누르기가 죽을 뿐이다 → `.simultaneousGesture(LongPressGesture())`로 나란히 돌린다 |
| 탭해도 아무 일이 없다 (콘솔에 `navigationDestination ... is misplaced`) | `NavigationSplitView`의 칸은 **저절로 `NavigationStack`이 되지 않는다.** 스택 없이 `navigationDestination`을 쓰면 SwiftUI는 그것을 "다음 칸"에 대한 지시로 읽는다 — 오른쪽 칸 다음에는 칸이 없다 → 밀고 들어가는 칸은 `NavigationStack`으로 감싼다. 왼쪽 칸에서 쓰면 오른쪽 칸을 갈아 끼우겠다는 뜻이 되어 선택과 다투므로, 거기서는 모달로 띄운다 |
| 사진 아래 글자가 칸 밖으로 나간다 | `scaledToFill`이 만든 큰 크기가 부모로 올라간 것. **`.frame(maxWidth: .infinity)`로는 못 막는다** — 상한이 무한이라 줄이는 힘이 없고, 자식이 더 크겠다고 하면 그 크기가 그대로 올라온다. `.clipped()`도 그리는 것만 자를 뿐 크기를 되돌리지 않는다 → 제안을 그대로 받아들이는 것(`Color.clear`) 위에 `.overlay`로 얹는다. overlay는 부모 크기에 영향을 주지 못한다 |
| `navigationTitle`을 줬는데 제목이 안 나온다 | 같은 툴바에 `ToolbarItem(placement: .principal)`이 있다. `.principal`은 제목이 놓이는 바로 그 자리라 제목을 **대체한다.** 지도 화면이 거기에 Apple/Google/Naver 선택기를 두고 있어서, 보드 이름도 "선택한 장소"도 처음부터 화면에 없었다 → 제목으로 알릴 수 없으면 화면 안에 띠(`scopeBanner`)로 얹는다 |
| 좁히기를 풀었는데 여전히 일부만 보인다 | 같은 화면을 좁히는 길이 **둘**이었다 — 한 번 걸어 두는 `mapFilterIDs`와 계속 따라다니는 `liveSelection`. 한쪽을 풀어도 다른 쪽이 남는다 → 좁히는 길은 하나로 두고, 나머지는 지운다 |
| 탭을 옮기자마자 방금 건 값이 사라진다 | `TabView`는 탭을 옮길 때 **떠나는 탭에 `.onDisappear`를 준다.** 화면이 물러나며 정리하는 코드가 거기 있으면, 탭을 누른 그 순간 정리돼 버린다 — 값을 건너편 탭이 읽으려던 참이면 그게 바로 기능이 죽는 순간이다. `selectedTab`을 보고 비켜 가려 해 봤지만 그대로 죽었다 → **탭을 건너다니는 값은 생명주기에 걸지 말고, 사용자의 조작으로 바뀌는 상태(`.onChange`)에 건다**(`GalleryView.syncLiveSelection`) |
| 좁은 화면에서만 옛 상태가 딸려 온다 | 뷰모델은 홈이 들고 있어 화면보다 오래 사는데, 좁은 화면에서는 목록에서 무엇을 고를 때마다 그 화면이 **새로 만들어진다.** 그때 값은 이미 정해진 뒤라 `.onChange`가 울릴 일이 없다. 넓은 화면은 반대로 화면이 그대로 살아 있어 `.onAppear`가 울리지 않는다 → **둘 다 둔다** |
| 그 둘 다 두었더니 이번엔 아예 안 걸린다 | 위 처방대로 `.onAppear`와 `.onChange` 양쪽에 "맞추고 비우는" 일을 두면, 한 번에 둘 다 지나가는 경우에 **뒤에 오는 쪽이 앞에서 건 것을 비운다.** 둘 사이의 순서는 보장되지 않으므로 어느 쪽에 맞춰 짜도 반대 경우에 깨진다 → 양쪽에는 **값을 넣어 주는 일만** 두고, 딸려 오면 안 되는 것을 놓는 규칙은 **뷰모델의 `didSet`에** 붙인다(`GalleryViewModel.scope`). 넣어 주는 자리가 몇이든, 어떤 순서로 지나가든 같아진다 |

마지막 여덟(탭이 안 먹는 것 둘, 글자가 넘치는 것, 제목이 안 나오는 것, 좁히기가
안 풀리는 것, 탭을 옮기며 값이 사라지는 것, 그리고 화면 생명주기와 얽힌 마지막
두 줄)은 전부 **컴파일은 멀쩡히 되고 실행할 때만 드러난다.** CI가 잡아
주지 못하므로 기기에서 봐야 한다. `navigationDestination` 건만 콘솔에 SwiftUI가
Fault를 내 주고 그 메시지가 원인을 정확히 짚어 준다 — 증상만으로 헤매지 말고
로그를 먼저 볼 것. 나머지 넷은 로그조차 조용하다.

---

## 6. 문서 상태

| 문서 | 상태 |
|---|---|
| `CLAUDE.md` | **최신.** 작업 규칙. 반드시 읽을 것 |
| `CHANGELOG.md` | **최신(256차, 빌드 49).** "왜 이렇게 됐나"는 여기. UI 개편(214차)부터 백업 메모리 크래시 수정(255·256차)까지 들어와 있다 |
| `00_UI개편_기초.md` | 이 문서. 지금 화면과 지켜야 할 규칙. 이름만 "개편"이다 |
| `00_다른_계정에서_이어받기.md` | **최신.** 다른 Apple 계정/다른 사람이 이어받을 때 바꿀 식별자. App Group·iCloud 컨테이너 둘 다 **셋**이고 빠뜨리면 공유/동기화가 조용히 죽는다. Team ID는 이제 커밋돼 있다(§3) |
| `00_인수인계_현재상태.md` | **최신(2026-09-23, 254차까지 반영).** §4·§6을 다시 썼다. §8 파일 지도만 PR #43 시점 그대로 낡았다 |
| `00_프로젝트종합가이드_새세션용.md` | **폐기.** 구현 전 기획서다. "iOS 개발 8주 ⏳" 같은 로드맵이 그대로 있다 — 앱은 이미 나와 있고 빌드 49이다. 기술 결정 배경으로만 참고 |
| `PLANNING/`·`RESEARCH/`·`IMPLEMENTATION/` | 기획·조사 단계 문서. 구현과 어긋난 곳이 있다(예: Naver 백엔드 프록시는 폐기됐고 앱이 NAVER API HUB를 직접 부른다). 배경으로만 |

### 아직 안 된 것 (256차 기준)

- **TestFlight 배포.** Xcode 서명이 걸려 있다 — `00_다른_계정에서_이어받기.md`
  §3. `DEVELOPMENT_TEAM`은 이제 커밋돼 있지만, 이어받는 사람은 Xcode에 그 팀의
  Apple ID로 로그인돼 있어야 한다(안 그러면 §3의 "Unknown Name" 증상).
- **"게시판 내보내기"·"게시판 가져오기"가 아직 옛 형식이다**(256차에서 남김).
  사진을 base64로 JSON에 인라인하므로 게시판 하나가 크면 같은 이유로 죽을 수
  있다. 전체 라이브러리보다 작은 부분집합이라 위험이 낮고, 내보내기와
  가져오기가 한 쌍으로 맞물려 있어 같이 바꿔야 한다.
- **동기화가 자동이 아니다.** 설정이나 갤러리 툴바에서 손으로 눌러야 한다.
- 서버의 고아 `PhotoAsset` 레코드를 안 치운다(236차).
- 카테고리를 보다가 "모든 카드"를 다시 눌러도 필터가 그대로다. `List(selection:)`이
  이미 골라 둔 줄을 다시 눌러도 알려 주지 않아 구별할 신호가 없다(221차).
