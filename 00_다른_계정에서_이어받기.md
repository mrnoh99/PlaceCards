# 다른 Apple 계정 / 다른 사람이 이어받을 때

**2026-09-20, 빌드 13 (`main` = PR #75) 기준.**

코드는 그대로 clone하면 되지만, **계정에 묶인 식별자 네 가지**는 그대로 쓸 수
없다. 이 문서는 그것만 다룬다. 앱이 무엇인지·어떤 키가 필요한지는
`README.md`, 작업 규칙은 `CLAUDE.md`를 보면 된다.

---

## 먼저 정할 것: 같은 팀인가, 다른 팀인가

### A. 같은 Apple Developer 팀에 새 사람을 추가한다 — **훨씬 쉽다**

팀 관리자가 App Store Connect에서 초대하면 끝이다. **식별자를 하나도 바꾸지
않는다.** 새 사람은 Xcode에 자기 Apple ID로 로그인하고 Team에서 그 팀을 고르면
된다. 가능하면 이 쪽을 택할 것.

### B. 완전히 다른 팀으로 간다 — **식별자를 전부 바꿔야 한다**

번들 ID는 전 세계에서 유일해야 하고 `com.mrnoh99.*`는 이미 기존 팀이
등록했다. **다른 팀은 그 이름을 등록할 수 없다.** 아래 §2를 전부 해야 한다.

---

## 1. 지금 쓰고 있는 식별자

| 무엇 | 값 | 어디에 |
|---|---|---|
| 앱 번들 ID | `com.mrnoh99.PinSpots` | `project.pbxproj` ×2 |
| 확장 번들 ID | `com.mrnoh99.PinSpots.Share` | `project.pbxproj` ×2 |
| App Group | `group.com.mrnoh99.PlaceCards` | 양쪽 `.entitlements` + **Swift 코드 1곳** |
| iCloud 컨테이너 | `iCloud.com.mrnoh99.PlaceCards` | 앱 `.entitlements` (키 2개에 각각) |
| Team ID | `492X57LLB4` | **커밋 안 되어 있음** (§3) |

---

## 2. 다른 팀으로 갈 때 바꿔야 할 곳 — 하나라도 빠뜨리면 조용히 깨진다

`com.mrnoh99` → 새 네임스페이스(예: `com.example`)로 바꾼다고 할 때:

### 2.1 번들 ID 4곳
`PlaceCards/PlaceCards.xcodeproj/project.pbxproj`의
`PRODUCT_BUNDLE_IDENTIFIER` 네 줄 (앱 Debug/Release, 확장 Debug/Release).
**확장은 반드시 앱 번들 ID의 하위여야 한다** — `<앱ID>.Share` 형태를 유지할 것.

### 2.2 App Group — **두 곳이 아니라 세 곳이다**
- `PlaceCards/PlaceCards/PlaceCards.entitlements`
- `PlaceCards/PlaceCardsShare/PlaceCardsShare.entitlements`
- ⚠️ **`Services/SharedImportStore.swift`의 `appGroupID` 상수**

> **가장 빠뜨리기 쉬운 곳이 세 번째다.** entitlements만 바꾸고 Swift 상수를
> 두면 **컴파일은 되고 앱도 뜨는데 "공유하기"가 조용히 아무 일도 안 한다.**
> 확장이 쓴 파일을 앱이 다른 컨테이너에서 찾기 때문이다. 오류도 안 난다.
> 그 파일의 `isAppGroupAvailable` 주석에 같은 경고가 적혀 있다.

Xcode에서 두 타깃 모두 Signing & Capabilities → App Groups에 새 그룹을 만들고
체크해야 한다. **양쪽 타깃 다.**

### 2.3 iCloud 컨테이너 2곳
`PlaceCards.entitlements` 안에서 두 키가 각각 들고 있다:
`com.apple.developer.icloud-container-identifiers`,
`com.apple.developer.ubiquity-container-identifiers`. 값은 같아야 한다.
Xcode에서 iCloud capability → iCloud Documents 체크 → 컨테이너 생성.

컨테이너가 없거나 권한이 없으면 `CloudBackupService`는 **조용히 아무것도 안
한다**(`resolveContainerDocumentsURL()`이 nil). 크래시가 아니라 무동작이라
알아채기 어렵다.

### 2.4 확인
바꾼 뒤 이게 0을 돌려줘야 한다:

```sh
grep -rn "com\.mrnoh99" --include='*.swift' --include='*.entitlements' \
  --include='*.pbxproj' PlaceCards/
```

그리고 **실기기에서** 다음 셋을 직접 해봐야 한다 — 시뮬레이터나 CI로는
확인되지 않는다:
1. 사진 앱에서 사진 하나를 PinSpots로 **공유** → 카드 만들기 화면이 뜨는가
   (App Group)
2. 설정에서 "iCloud에 자동 보관"을 켜고 앱을 닫았다 열기 (iCloud 컨테이너)
3. 앱 삭제 후 재설치 → 콜드 런치에 iCloud에서 복원되는가

---

## 3. Team ID는 일부러 커밋하지 않았다

`DEVELOPMENT_TEAM`은 `project.pbxproj`에 **없다.** 각자 Xcode의 Team
드롭다운에서 고른다.

한 번 커밋해봤다가 되돌렸다(PR #74 → #75). 그 계정의 Apple ID가 Xcode에
로그인돼 있지 않으면 `Unknown Name (…)` / `No Account for Team` 오류로
**빌드가 막힌다.** 여러 사람이 각자 다른 팀으로 빌드할 거라면 커밋하지 않는
쪽이 맞다.

**대가**: `project.pbxproj`가 자주 바뀌므로(빌드 번호) `git pull`마다 Team을
다시 지정하게 될 수 있다. 한 팀으로 고정된다면 그때 커밋하면 되고, 여럿이
쓸 거면 `.xcconfig`로 빼서 `.gitignore`에 넣는 쪽이 정석이다.

### Signing이 빨갛게 뜰 때
1. **Xcode → Settings → Accounts**에 Apple ID가 있는가. 없으면 추가.
2. `security find-identity -v -p codesigning` — 비어 있으면 인증서가 없는 것.
3. ⚠️ **로컬 `project.pbxproj`를 grep해서 Team ID를 알아내려 하지 말 것.**
   커밋된 값을 되읽을 뿐이라 순환이다. 실제로 그렇게 물어봤다가 헛짚었다.

---

## 4. API 키는 애초에 저장소에 없다 (BYOK)

Google Places, AI 제공자, Naver 두 쌍 — 전부 각자 발급받아 앱의 **설정**
탭에서 등록한다. Keychain에만 저장되고 iCloud로 동기화되지 않으므로
**새 사람은 자기 키를 새로 넣어야 한다.** 어떤 키가 왜 필요한지는
`README.md`의 "실행에 필요한 API 키" 표에 있다.

`CLAUDE.md`와 `00_인수인계_현재상태.md` §2에 Google 키 관련 금지사항이 있다
(iOS 앱 제한 걸지 말 것, Geocoding API 켜지 말 것, 필드 마스크가 비용을
정한다) — **키를 새로 발급할 때 반드시 읽을 것.**

---

## 5. 문서 읽는 순서

1. `CLAUDE.md` — 작업 규칙. CI가 유일한 컴파일 검증이라는 것부터.
2. 이 문서 — 계정 이관.
3. `README.md` — 앱 소개, 빌드, 필요한 키.
4. `00_UI개편_기초.md` — **UI를 건드린다면.** 화면 지도와, UI를 다시 써도
   살아남아야 하는 동작 목록.
5. `CHANGELOG.md` — 어떤 결정이 왜 그렇게 됐는지. 가장 믿을 만하다.

`00_인수인계_현재상태.md`는 PR #43에서 멈췄고,
`00_프로젝트종합가이드_새세션용.md`는 구현 전 기획서다. 각각 맨 위에 경고가
붙어 있다.
